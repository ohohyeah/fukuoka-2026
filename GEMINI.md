# 福岡旅行文件專案 - Antigravity 專案規範

## Git Commit 訊息規範
當使用 Antigravity 生成 Git Commit 訊息（包含點擊 Source Control 側邊欄的 AI Commit 按鈕或對話面板的 commit 指令）時，請遵守以下規則：

1. **語言與格式**：
   - 必須使用**繁體中文單行摘要**。
   - 不包含 Conventional Commits 前綴（例如不要加 `docs:`）。

2. **分析與摘要原則**：
   - 忽略純粹的排版調整、換行增減與空行格式化。
   - 著重於實質內容的異動摘要（行程、地點、機票住宿訂位、費用預算、行李清單等）。
   - 以動詞開頭，簡明精準，字數控制在 50 字以內。
